To run this program

You need to 1st run frame.py so frames will get extracted in frames Folder.

Then run detection.py so it will detect vehicals in image and rename the image with number of vehicals.

##This detection model is not that perfect it should be changed##
